// if (include_fields.includes('metadata')) {
//   metadata_fields.forEach((field: any) => {
//     const external_id = field.external_id;
//     const label = field.label;
//     if (asset.metadata && asset.metadata[external_id]) {
//       additional_data[label] = label; // Use label as the value
//     } else {
//       additional_data[label] = ''; // Use empty string if metadata is missing
//     }
//   });
//   console.log(additional_data);
// }
